export PATH="@defaultpath@:@libexecdir@/riot"

export BUILDDIR="@builddir@"
export CACHEDIR="@cachedir@"
export INSTALLDIR="@installdir@"
export REPODIR="@repodir@"

export CHOST="@host@"
export CTARGET="@target@"
export CBUILD="@build@"

ALLOWED_VARIABLES+=( PATH BUILDDIR CACHEDIR INSTALLDIR REPODIR CHOST CTARGET CBUILD )

