#!/bin/sh

[ "${PRAXIS_ENV}" = 1 ] || exec env -i PRAXIS_ENV=1 PATH="${PATH}" praxis "$@"

PRAXIS_SUBCOMMAND="${1:-help}"; shift 2>/dev/null

for f in "@@libdir@@"/*.sh; do
	. "${f}"
done

PRAXIS_PID="$$"

. praxis-"${PRAXIS_SUBCOMMAND}" "$@"

