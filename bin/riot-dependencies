#!/bin/sh

root_pkg="${1}"; shift
root_pkg_path=$(riot-path "${root_pkg}")
dependencies=($(riot-read "${root_pkg_path}" dependencies))

for pkg in "${dependencies[@]}";do
    dependencies+=($(riot-dependencies "${pkg}"))
done

printf '%s\n' "${dependencies[@]}"

