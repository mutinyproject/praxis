# repository layout

```
<pkgdir>/
    <repository>/
        data/
            importance
        [lib]/  - optional directory, for functions for build systems, etc.
            autotools
            cmake
            python
        pkg/    - packages
            <package>/
                [files]/    - optional directory, for various files, patches...
                <package>
```
